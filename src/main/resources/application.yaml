spring:
  application:
    name: WishList
  
  datasource:
    url: jdbc:mysql://localhost3360/WishList
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
        
server:
  port: 8080
